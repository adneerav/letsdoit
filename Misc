from movies.serializers import ContentSerializer,GenreSerializer
from movies.models import Content,Genre
from rest_framework.renderers import JSONRenderer
from rest_framework.parsers import JSONParser
from django.db.models import Q



To get content by genre id
   1) contents = Content.objects.filter(genres__id="1")
   2) comedy_genre = Genre.objects.get(pk=1)
        contents = comedy_genre.contents.all()


